{% extends "html/base.html.j2" %}

{% block content %}
    <h1>Zaloguj</h1>
    <form action="" method="post" name="login" novalidate >
        {{ form.hidden_tag() }}
        <p>
            {{ form.username.label }}<br>
            {{ form.username(size=32) }}
        </p>
        <p>
            {{ form.password.label }}<br>
            {{ form.password(size=32) }}
        </p>

        <p>{{ form.submit() }}</p>
    </form>
    <form action="" method="post" name="register" novalidate >
        <p>{{ form2.submit() }}</p>
    </form>

{% endblock %}